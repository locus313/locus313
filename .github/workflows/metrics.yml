name: Update README cards

on:
  schedule:
    - cron: "0 3 * * *"   # Runs daily at 3am UTC
  workflow_dispatch:        # Allow manual runs

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Generate stats card
        uses: locus313/metrics@a6a8b51adb35b0e7e50fe924ee4080013b8b09bc
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: assets/stats.svg
          base: "header, repositories, commits, contributions"
          use_prebuilt_image: false

      - name: Generate top languages card
        uses: locus313/metrics@a6a8b51adb35b0e7e50fe924ee4080013b8b09bc
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: assets/top-langs.svg
          base: ""
          plugin_languages: yes
          plugin_languages_limit: 8
          use_prebuilt_image: false

      - name: Generate achievements card
        uses: locus313/metrics@a6a8b51adb35b0e7e50fe924ee4080013b8b09bc
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: assets/metrics.achievements.svg
          base: ""
          plugin_achievements: yes
          plugin_achievements_display: compact
          plugin_achievements_secrets: yes
          plugin_achievements_limit: 0
          use_prebuilt_image: false

      - name: Commit cards
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@users.noreply.github.com"
          git add assets/*.svg
          git commit -m "Update README cards" || exit 0
          git pull --rebase
          git push
